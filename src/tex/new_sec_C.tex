\subsection{The WDM Covariance Matrix in the stationary case (without circulant approximation)}

We start from the WDM transform in Eq. \eqref{wnm_def}. It can be re-written in the time domain by injecting the expression of the DFT of $x$ and $g$, which yields
\begin{equation}
\label{eq:wdm-transform-time-domain}
    w_{nm} = N \sum_{k=0}^{N-1} x[k] g_{nm}[k].
\end{equation}
The WDM covariance matrix elements are then
\begin{align}
(\boldsymbol{\Sigma}_w)_{(n,m),(p,q)} &= \mathbb{E}[w_{nm}w_{pq}] = N^2 \sum_{k,k'}g_{nm}[k]g_{pq}[k']\mathbb{E}[x[k]x[k']].
\end{align}
For a wide-sense stationary process, the covariance term is given by the auto-covariance function
\begin{equation}
\label{eq:autocovariance}
R(\kappa) = \mathbb{E}[x[k]x[k+\kappa]],
\end{equation}
which only depends on the time separation between the two data points.
The Wiener-Khinchin theorem states that the Fourier transform of the
time-domain auto-covariance function is related to one-sided power spectral density as
\begin{equation}
\label{eq:wiener-khinchin}
    R(\kappa) = \frac{1}{2}\int_{-\frac{f_s}{2}}^{\frac{f_s}{2}} S(f) e^{2 i \pi f \kappa \Delta t } df.
\end{equation}
Using Eqs. \eqref{eq:autocovariance} and \eqref{eq:wiener-khinchin} into \eqref{eq:wdm-transform-time-domain} gives
\begin{align}
(\boldsymbol{\Sigma}_w)_{(n,m),(p,q)} &=  \frac{N^2}{2}\int_{-\frac{f_s}{2}}^{\frac{f_s}{2}} S(f)  \sum_{k,k'}g_{nm}[k]g_{pq}[k'] e^{2 i \pi f (k'-k) \Delta t } df \\
& = \frac{N^2}{2}\int_{-\frac{f_s}{2}}^{\frac{f_s}{2}} S(f) \tilde{g}_{nm}(f) \tilde{g}_{pq}^{*}(f) df.
\end{align}
\qb{Then it is the same rationale as Ollie above}\gm{That's great, let's say what Ollie decides. Also, could you please move to the discrete domain? We never had continuous domain in the rest of the draft} The atoms from the Wilson basis $\tilde{g}_{nm}$ are peaked around the frequency $f_m = m N_t \Delta f/2$. This is where the support of the Wilson basis lies. This means that the dominant contribution of the integral are for those frequencies in the vicinity of $f_m$ and $f_q$. Let us now assume that the PSD varies slowly enough so that we can assimilate it to a constant equal to its value at $(f_m + f_q) / 2$:
\begin{align}
\label{eq:constant-psd-approx}
(\boldsymbol{\Sigma}_w)_{(n,m),(p,q)} & \approx \frac{N^2}{2} S\left((f_m + f_q) / 2\right)\int_{-\frac{f_s}{2}}^{\frac{f_s}{2}} \tilde{g}_{nm}(f) \tilde{g}_{pq}^{*}(f) df.
\end{align}
Since the PSD is now out of the integral, the WDM covariance is proportional to the continuous version of the orthogonality condition for interior channels in Eq.~\eqref{eq:orthonormality}.
Hence,
\begin{equation}
    \int_{-\frac{f_s}{2}}^{\frac{f_s}{2}} \tilde{g}_{nm}(f) \tilde{g}_{pq}^{*}(f) df 
    = \sum_{l=0}^{N-1}  \tilde{g}_{nm}(f[l]) \tilde{g}_{pq}^{*}(f[l']) \Delta f = \delta_{np} \delta_{mq} \Delta f.
\end{equation}
Injecting this into Eq. \eqref{eq:constant-psd-approx} yields
\begin{align}
\label{eq:wdm-covariance-stationary}
(\boldsymbol{\Sigma}_w)_{(n,m),(p,q)} & = \frac{N}{2\Delta t} S(f_m) \delta_{np} \delta_{mq} \;\;\text{for}\;\; 0<m<N_f.
\end{align}