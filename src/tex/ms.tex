\documentclass[
  reprint,
  superscriptaddress,
  nofootinbib,
  amsmath,
  amssymb,
  aps,
  colorlinks=true,
  linkcolor=blue,
  urlcolor=blue,
  citecolor=blue,
  anchorcolor=blue
]{revtex4-2}

\usepackage{wdmmanuscript}
\usepackage{hyperref}

\begin{document}

\preprint{APS/123-QED}

\title{Python-based codes for WDM-transform for use within gravitational wave astronomy}

\author{Quentin Baghi}
\affiliation{Universit\'e Paris Cit\'e, CNRS, Astroparticule et Cosmologie, F-75013 Paris, France}
\author{Ollie Burke}
\affiliation{School of Physics and Astronomy, University of Glasgow,
University Avenue, Glasgow, G12 8QQ, UK}
\affiliation{Laboratoire des 2 Infinis - Toulouse (L2IT-IN2P3),
Université de Toulouse, CNRS, UPS, F-31062 Toulouse Cedex 9, France}
\author{Giorgio Mentasti}
\affiliation{Universit\'e Paris Cit\'e, CNRS, Astroparticule et Cosmologie, F-75013 Paris, France}
\author{Lorenzo Speri}
\author{Avi Vajpeyi}
\affiliation{Department of Statistics, University of Auckland, 38
Princes St, Auckland, New Zealand}

\begin{abstract}
  Here we present \package, a Python package for rapid and accurate
  Wilson-Daubechies-Meyer (WDM) time-frequency transforms on CPU and
  accelerator backends. The current implementations use NumPy and JAX
  and can transform million-point data streams in milliseconds while
  maintaining excellent numerical accuracy. We give a practical
  introduction to the WDM domain, discuss several implementation
  choices and limitations, and illustrate how these methods can
  support future space-based gravitational-wave data analysis.
\end{abstract}

\maketitle


\section{TODOs}

\begin{itemize}
    \item Orthonormality of Gnm (plot avi)
    \item forward and inverse transform (giorgio)
    \item the WDM coef should be set by the noise and galactic background
    \item (for docs animate T going up and down)
    \item write about the galactic binary example plots corner plots
    \item writing covar in presence of gaps, non stationary noise, is under investigation, crucial for LISA analysis. 
    \item having good orthonormaity allows you to transform domains
\end{itemize}


\section{Introduction}





Time-frequency analysis is widely used in gravitational-wave
astronomy. It enables template-free modeling of source
signals~\cite{krolak_1996,Virtuoso2024}, instrumental
transients~\cite{robinet2020}, and the joint analysis of
astrophysical signals and terrestrial
disturbances~\cite{Cornish_2015}. Moreover, time-frequency
representations provide compact waveform descriptions and facilitate
the modeling of non-stationary detector noise. Among the available
techniques, discrete wavelet wavepacket transforms are particularly
well suited to gravitational-wave data analysis: they furnish
localized, orthonormal basis functions capable of representing
transient and chirping signals, and for locally stationary noise they
yield an approximately diagonal noise covariance, greatly simplifying
likelihood evaluation and spectral estimation.

The Wilson--Daubechies--Meyer (WDM) wavelet wavepacket
basis~\cite{Necula_2012} was introduced into gravitational-wave
analysis through the coherent WaveBurst
algorithm~\cite{Klimenko:2005xv,Klimenko:2015ypf} for unmodeled burst
searches. Building on this foundation, Cornish~\cite{Cornish:2020odn}
developed WDM into a general-purpose framework for template-based
inference, demonstrating that binary merger waveforms can be
generated directly in the WDM domain at a computational cost scaling
as $N\sqrt{N}$ rather than $N^2$, and showing that the WDM noise
covariance is diagonal for a broad class of locally stationary
processes. These results provide strong motivation for performing
gravitational-wave inference directly in the WDM domain, particularly
for long-duration signals expected in future detectors such as LISA.

While the foundational works above establish the theoretical and
algorithmic advantages of the WDM transform, many practical aspects
of its implementation are necessarily distributed across the
literature and existing codebases. In particular, details such as the
packed coefficient layout, the treatment of the DC and Nyquist edge
channels, and the numerical properties of the discretized transform
are often implicit. As a result, adopting WDM in new applications can
require reconstructing these elements from multiple sources.

This paper aims to provide a unified and pedagogical presentation of
the WDM transform. We describe what each coefficient represents, how
the forward and inverse transforms are computed step by step, how the
packed $(N_t, N_f+1)$ coefficient grid is organized in practice, and
under what conditions the coefficients may be treated as
approximately independent. We accompany this exposition with
\package, an open-source Python package that implements the transform
on both CPU (NumPy) and accelerator (JAX) backends, with validation
through round-trip reconstruction tests and empirical decorrelation studies.

The motivation for WDM can be understood by comparing it with the two
standard representations of a discrete signal. In the time domain one
can identify \emph{when} a feature occurs but not which frequency
band is active, while in the Fourier domain one can identify
\emph{which} frequencies are present but not when they appear. Many
gravitational-wave signals and detector artifacts are neither purely
stationary nor purely impulsive, so neither representation is fully
satisfactory in isolation. A short-time Fourier transform partially
addresses this limitation through a sliding window, but imposes a
fixed time-frequency resolution across the dataset. The WDM transform
instead tiles the time-frequency plane with localized atoms indexed
by a time bin $n$ and a frequency channel $m$, with coefficients
$w_{nm}$ measuring the projection of the data onto each atom. The
result is a uniform time-frequency representation that is well suited
to describing transients, chirps, gaps, and other localized
structure, while still admitting an exact inverse transform back to
the original sampled data.

The remainder of this paper is organized as follows.
Section~\ref{sec:conventions} introduces notation and conventions.
Section~\ref{sec:wdm} develops the WDM transform, including the Meyer
window, the full three-case basis definition covering DC, interior,
and Nyquist channels, the forward and inverse transforms, and the
statistical properties of the coefficients under stationary and
non-stationary noise. Section~\ref{sec:code_package} describes the
\package\ package, presents runtime and accuracy benchmarks, and
demonstrates the transform on astrophysical signals. We conclude in
Section~\ref{sec:conclusions}..

\subsection{Conventions}\label{sec:conventions}
We are given a data stream in time domain, consisting of a set of N
samples of a \textbf{real} quantity $x$ at times $\{t_k\}_{k=0\dots
N-1}$ in a total time of observation $T$
\begin{align}\label{st_definition}
  x[k]=x(t_k)\qquad,\qquad k=\{0\dots N-1\}\;,
\end{align}
sampled uniformly with sampling interval $\Delta t = t_{k+1} - t_{k}$
with $T = N\Delta t$. All of the information we have lies in these N
points. We define the time sampling rate and the Nyquist frequency as
\begin{align}
  r_s&\equiv\frac{1}{\Delta t}\equiv\frac{N}{T}\;,\nonumber\\
  f_{\rm max}&=\frac 1 2r_s\;,\nonumber\\
  \Delta f &= 1/T
\end{align}
With that respect, if we set $t_0=0$, we can write $t_k=k\Delta t$.
In the frequency domain, the data stream $\{x_k\}$ can be
discrete-Fourier-transformed to a set of other $N$ complex numbers as
\begin{equation}
  \label{eq:dft-definition}
  X[p] = \sum_{k = 0}^{N-1} x[k]e^{-2\pi i p k/N}
\end{equation}
for Fourier frequencies $f_{l} = \{-N\Delta f/2, \ldots, -\Delta f,
0, \Delta f, (N/2 - 1)\Delta f\}$. This is convenient since the
zeroth frequency bin has been shifted into the middle of the spectrum.

\subsection{Time and Frequency Domains}
We assume that the output of a gravitational wave detector, $x(t)$,
is expressed as a linear combination of a signal, $h(t| \vec{\mu})$,
determined by a finite set of (unknown) parameters, $\vec{\mu}$, and
instrumental noise, $n(t)$. If we ignore the presence of calibration
errors~\cite{Savalle_2022}, the content of a single data stream,
i.e., one output channel from one detector, can be written in the
time and frequency domain as:
\begin{align}
  {x}[k] & = h[k|\vec{\mu}]+ n[t],\\
  X[l]& = \tilde{h}[l|\vec{\mu}]+ \tilde{n}[l]
\end{align}
where the tilde indicates the Fourier transform.

In a gravitational wave context it is usual to further assume that
the instrumental noise follows a Gaussian distribution characterized
by a one-sided PSD, $S_n(f)$, defined such that
\begin{equation}\label{eq:psd_definition}
  \langle \tilde{n}^*[l] \tilde{n}[l'] \rangle = \frac{1}{2} P[l] \delta_{ll'},
\end{equation}
where the expectation value $\langle \, \rangle$ is taken over the
data generating process. The delta function in the previous equation
implies that different frequencies are not correlated.

In reality the noise model is not known perfectly and could vary from
the assumption above in a number of ways. For example, the PSD might
have a different shape from the reference one \cite{babak2021lisa},
the probability distribution of the noise might not be Gaussian, or
the noise might not be stationary, leading to correlations between frequencies.

\section{The Wilson-Daubechies-Meyer (WDM) Transform}\label{sec:wdm}

Before defining the transform, it is worth clarifying a point of
terminology. A standard discrete wavelet transform (DWT) decomposes a
signal using a dyadic tiling of the time-frequency plane: each
successive scale doubles the frequency bandwidth and halves the time
resolution. This is natural for self-similar or fractal-like signals,
but it means that low-frequency channels have coarse time resolution
and high-frequency channels have coarse frequency resolution. A
\emph{wavelet packet} (or \emph{wavelet wavepacket}) transform
generalizes this by allowing the user to choose any admissible tiling
of the time-frequency plane. The WDM basis is a specific wavelet
packet basis that uses a \emph{uniform} rectangular tiling: every
cell has the same width $\Delta T$ in time and the same width $\Delta
F$ in frequency, with $\Delta T\,\Delta F = 1/2$. This uniform tiling
is what makes the WDM representation so well suited for
gravitational-wave data analysis, because signals that sweep across
many frequency bands (such as compact binary inspirals) occupy a
simple, coherent track on the grid rather than being spread across
cells of very different sizes. Throughout this paper, we use ``WDM
transform'' and ``wavelet transform'' interchangeably, but the reader
should keep in mind that the WDM basis belongs to the wavelet packet
family rather than to the standard dyadic wavelet family.

The WDM transform maps the original $N$ samples to a packed
coefficient grid with $N_t$ time bins and $N_f+1$ stored frequency
channels. The transform size is still set by
\begin{align}\label{NtNf_def}
  N=N_t\,N_f\;,
\end{align}
and the stored channels are indexed by $m=0,\dots,N_f$, with $m=0$
the DC edge channel, $m=1,\dots,N_f-1$ the interior channels, and
$m=N_f$ the Nyquist edge channel. The underlying idea is to chunk the
data stream into $N_t$ chunks and to perform a frequency analysis of
each one of those. Please note that $N$ is fixed, but $N_t$ is an
arbitrary choice you can make. The only constraint is obviously $1\le
N_t\le N$. Consequently, the constraint applies for $N_f$: $1\le N_f\le N$.
Given that choice we can define the spacing in both time and
frequency with respect to the underlying tiling with area $N = N_{t}N_{f}$
\begin{align}\label{DTDF}
  \Delta T&\equiv\frac{T_{\text{obs}}}{N_t}\; \qquad \text{grid time
  spacing},\nonumber\\
  \Delta F&\equiv\frac{f_{\rm max}}{N_f} \qquad \text{grid frequency spacing}\,.
\end{align}
One immediately sees that,
\begin{align}
  \Delta T &= \frac{T_{\text{obs}}}{N_{t}} = \frac{N\Delta t}{N_{t}}
  = N_{f}\Delta t \\
  \Delta F &= \frac{f_{\text{max}}}{N_{f}} = \frac{1}{2N_{f}\Delta t}=\frac{N_t\Delta f}{2} = \frac{1}{2\Delta T}
\end{align}
This final expression shows how the Nyquist theorem still holds. Once
you fix $N_t$ (or equivalently $N_f$, $\Delta T$ or $\Delta F$) the
grid is uniquely identified. Figure~\ref{fig:wdm_packed_grid}
summarizes the packed layout, including the DC and Nyquist edge
channels and the alternating phase factor used for the interior
coefficients.

\begin{figure*}[t]
  \centering
  \includegraphics[width=\linewidth]{figures/wdm_packed_grid.pdf}
  \caption{\textbf{Packed WDM coefficient layout.} The left panels
    sketch the same data in the one-sided frequency series and in the
    time domain, emphasizing the frequency index range $k=0,\ldots,N/2$
    and the time-bin spacing $\Delta T=T_{\mathrm{obs}}/N_t$. The
    center panel shows the packed WDM coefficient array $W[n,m]$, whose
    stored channel index runs from $m=0$ to $m=N_f$, so the array has
    shape $N_t \times (N_f+1)$ even though the transform size is fixed
    by $N=N_tN_f$. The right panel shows the alternating phase factor
  $C_{nm}$, which equals $1$ when $n+m$ is even and $i$ when $n+m$ is odd.}
  \label{fig:wdm_packed_grid}
  \script{wdm_packed_grid.py}
\end{figure*}

\subsection{Forward transform}
For a given frequency series $\tilde x[l]$, the WDM transform represents the data
through the coefficients
\begin{align}
  \label{wnm_def}
  w_{nm}&=\sum_{l=0}^{N-1}\tilde x[l]\tilde g^*_{nm}[l]\;,\nonumber\\
  \tilde x[l]&=\sum_{n=0}^{N_t}\sum_{m=0}^{N_f}w_{nm}\tilde g_{nm}[l]\;,
\end{align}
where $n$ corresponds to the time index and $m$ corresponds to the
frequency (or scale) index. The functions $g_{nm}[k]$ are the WDM
basis elements, which are chosen to be (quasi-)orthonormal:
\begin{align}\label{eq:orthonormality}
\sum_{l=0}^{N-1}g^*_{nm}[l]g_{pq}[l]=\delta_{np}\delta_{mq}\;,
\end{align}
This orthonormality is also the key reason that WDM coefficients can
become statistically simple for stationary noise. If the
Fourier-domain noise satisfies Eq.~\eqref{eq:psd_definition}, then
the covariance between two WDM noise coefficients is
\begin{align}
  \left\langle w_{nm} w_{pq}^* \right\rangle
  = \frac{1}{2}\sum_l P[l]\,\tilde g_{nm}^*(l)\tilde g_{pq}(l).
\end{align}
In the ideal orthonormal limit, whitened noise, or more generally
noise with $P[l]$ effectively constant across the support of each
atom, reduces this covariance to a diagonal form. In that case the
WDM coefficients are uncorrelated, and for Gaussian noise they are
therefore independent. In the sampled packed implementation used
here, it is useful to separate two statements. First, the interior
atoms ($m=1,\ldots,N_f-1$) satisfy the discrete orthonormality
relation numerically to machine precision. Second, the packed
real-valued storage includes special DC and Nyquist edge channels,
so the full stored coefficient array is best viewed as a convenient
packed representation rather than as a minimal orthonormal basis.
For this reason, the decorrelation discussion below focuses on the
interior coefficients. For whitened noise their empirical
correlations remain very small, while for colored, non-stationary, or
non-Gaussian noise off-diagonal structure becomes more visible and
the coefficients should be regarded as only approximately decorrelated
rather than strictly independent.
Figure~\ref{fig:wdm_orthogonality_decorrelation} illustrates these points
numerically for a small sampled transform and for white and colored
noise realizations.

\begin{figure*}[t]
  \centering
  \includegraphics[width=1\linewidth]{figures/wdm_orthogonality_decorrelation.pdf}
  \caption{\textbf{Numerical checks of orthogonality and
    decorrelation.} Left: Gram matrix of the interior sampled WDM
    atoms, showing the discrete inner products
    $\langle g_i, g_j\rangle$ after flattening the interior index pair
    $(n,m)$ into a single label $i$. The matrix is visually
    indistinguishable from the identity, and the annotated residuals
    are at machine precision, providing a direct numerical
    demonstration of orthonormality for the interior channels.
    Middle: empirical correlation matrix of interior WDM coefficients
    estimated from many realizations of white Gaussian noise; most
    off-diagonal entries remain small. Right: the same calculation for
    colored stationary noise, where visibly stronger off-diagonal
  correlations appear.}
  \label{fig:wdm_orthogonality_decorrelation}
  \script{wdm_orthogonality_decorrelation.py}
\end{figure*}

The basis elements are defined in closed form in the frequency
domain. For interior channels ($0 < m < N_f$), DC ($m=0$), and
Nyquist ($m=N_f$) the definitions differ:
\begin{align}
  \label{eq:gnm_full}
  \tilde{g}_{nm}(f) =
  \begin{cases}
    e^{-4\pi i n f \Delta T}\, \tilde{\varphi}(f), & m=0, \\[6pt]
    \dfrac{1}{\sqrt{2}}\,e^{-2\pi i n f \Delta
    T}\Big(C_{nm}\,\tilde{\varphi}(f - m\Delta F) & \\[2pt]
    \quad + C_{nm}^*\,\tilde{\varphi}(f + m\Delta F)\Big), & 0 < m <
    N_f, \\[6pt]
    e^{-4\pi i n f \Delta T}\Big(\tilde{\varphi}(f - N_f\Delta F) & \\[2pt]
    \quad + \tilde{\varphi}(f + N_f\Delta F)\Big), & m = N_f,
  \end{cases}
\end{align}
for $n = 0,\ldots,N_t-1$. The interior channels carry a $1/\sqrt{2}$
normalization and the alternating phase factor $C_{nm}$, while the DC
and Nyquist edge channels have a doubled time-shift exponent and no
$C_{nm}$ modulation. These edge channels each contribute $N_t$ real
degrees of freedom to the packed grid, accounting for the
$N_t(N_f+1)$ stored entries versus the $N = N_tN_f$ independent samples.

The window $\tilde\varphi$ is the Meyer window, defined in the
frequency domain by
\begin{align}\label{eq:meyer_window}
  \tilde\varphi(\omega) &=
  \begin{cases}
    \frac{1}{\sqrt{\Delta \Omega}} & |\omega|<A \\
    \frac{1}{\sqrt{\Delta \Omega}} \cos \left[\frac{\pi}{2}\,\left(
    \frac{|\omega|-A}{B}\right)\right] & A \leq|\omega| \leq A+B \\
    0 & \text{otherwise,}
  \end{cases}
\end{align}
where $\omega = 2 \pi f$ are angular frequencies, which are discretized as
\begin{equation}
  \omega_l = \frac{2\pi l}{N \Delta t}
\end{equation}
By construction, $\tilde\varphi$ has a limited frequency bandwidth
which is controlled by the parameter $A$. In this work, we restrict
attention to the $d=1$ case, for which the Meyer window reduces to
the cosine taper written above; this is also the case implemented in
the current package, while $d$ is retained in the API only for
possible future
generalizations.\footnote{Cornish~\cite{Cornish:2020odn} uses $d=4$,
  which yields a flatter passband and steeper roll-off at the expense
  of a wider time-domain support (the window function must be truncated
  at $\pm q\Delta T$ with $q=16$). The $d=1$ cosine taper used here has
  a gentler roll-off but is more compact in time, requiring a smaller
  $q$. In practice, the choice of $d$ trades spectral leakage
  suppression against temporal compactness of the window; for the
  applications considered in this paper the $d=1$ window provides
  adequate frequency separation while keeping the implementation simple
and the time-domain filter short.} The parameter $\Delta \Omega$
depends on the frequency resolution
\begin{equation}
  \Delta\Omega = 2 \pi \Delta \tilde{F},
\end{equation}
where $\Delta \tilde{F} = \Delta F \Delta t$ is the normalized
frequency resolution.
The parameter $B$ depends on $\Delta\Omega$ and $A$ as
\begin{equation}
  B = \Delta\Omega - 2A
\end{equation}
In our implementation, we set $A =\Delta\Omega/4$ and therefore $B =
\Delta\Omega/2$. This means that the window is non-zero when
$|\omega| \leq 3 \Delta\Omega/4$. Figure~\ref{fig:wdm_windows_atoms}
summarizes the window shape, its shifted channel copies, and the
localized atoms that result after modulation in time.

\begin{figure*}[t]
  \centering
  \includegraphics[width=1\linewidth]{figures/wdm_windows_atoms.pdf}
  \caption{\textbf{Pedagogical view of the WDM window and atoms.}
    Left: the cosine-tapered Meyer window used in the current
    implementation. Middle: shifted copies of the same window define
    the WDM frequency channels, with special edge-channel behavior at
    DC and Nyquist. Right: after fixing a channel $m$ and time index
    $n$, the corresponding inverse-Fourier-transformed atom becomes a
    localized oscillatory pattern in time. This is the object that one
  WDM coefficient measures against the data.}
  \label{fig:wdm_windows_atoms}
  \script{wdm_windows_atoms.py}
\end{figure*}

These are the main ingredients behind the packed WDM grid. A single
window shape is shifted across
frequency to define the channel index $m$, and each shifted window is
then modulated in time to define the atom indexed by $n$. This is why
one WDM coefficient can be read as the strength of one localized
time-frequency pattern, rather than as the amplitude of a globally
supported sinusoid.

More concretely, a single coefficient $w_{nm}$ answers the question:
``how much does the data resemble the atom centered at time bin $n$
and frequency channel $m$?'' A large magnitude means that the data
contain a feature with roughly the same time localization, central
frequency, and oscillatory phase structure as that atom. Reading the
WDM plane coefficient-by-coefficient in this way is often much more
intuitive than interpreting a global Fourier series, because the
index pair $(n,m)$ already carries an approximate time-frequency location.

We now derive the practical computational form of the forward WDM
coefficients by inserting the basis functions~\eqref{eq:gnm_full}
into the definition~\eqref{wnm_def}. The phase factor $C_{nm}$
alternates between real and imaginary depending on $n+m$:
\begin{align}
  C_{nm}=e^{\frac{i\pi}{4}(1-(-1)^{n+m})} =
  \begin{cases} 1 & n+m \text{ even,} \\ i & n+m \text{ odd.}
  \end{cases}
\end{align}
We treat the three channel types separately.

\subsubsection{Interior channels ($0 < m < N_f$)}

Starting from Eq.~\eqref{wnm_def} with the interior-channel basis
element from Eq.~\eqref{eq:gnm_full}, the forward transform can be
evaluated in the Fourier domain using
\begin{align}
  w_{nm} = \sum_{l=0}^{N-1} \tilde{x}_l\,\tilde{g}_{nm}^*(f[l])\,,
\end{align}
where $\tilde{x}_l = X[l]$ is the DFT of the input data. For a
real-valued input signal, $\tilde{x}_{N-l} = \tilde{x}_l^*$, and the
window satisfies $\tilde{\varphi}(-f) = \tilde{\varphi}(f)$.
Splitting the sum into positive and negative frequency halves and
using these symmetries, the negative-frequency contribution is the
complex conjugate of the positive-frequency contribution (see
Appendix~\ref{app:forward_derivation} for details), giving
\begin{align}\label{eq:wnm_interior}
  w_{nm} &=
  \sqrt{2}\,(-1)^{nm}\,\Re\left[C_{nm}^*\sum_{l'=-N_t/2}^{N_t/2}\tilde{x}_{l'+mN_t/2}\,e^{2\pi
  i l'n/N_t}\,\tilde{\varphi}(l'\,df)\right],
\end{align}
where $l' = l - mN_t/2$ and $df = 1/T$. This can be written compactly as
\begin{align}\label{eq:wnm_compact}
  w_{nm} = \sqrt{2}\,(-1)^{nm}\,\Re\,C_{nm}^*\,x_m[n]\,,
\end{align}
where
\begin{align}\label{eq:xm_def}
  x_m[n] = \sum_{l=-N_t/2}^{N_t/2-1} e^{2\pi i l n/N_t}\,X[l +
  mN_t/2]\,\tilde{\varphi}[l]
\end{align}
is the windowed, shifted inverse DFT of length $N_t$. The quantity
$x_m[n]$ can be computed using a length-$N_t$ FFT for each channel
$m$, so the total cost of the interior channels is $N_f \times
\mathcal{O}(N_t\log N_t)$.

\subsubsection{DC channel ($m = 0$)}

The DC edge channel has a doubled time-shift exponent and no $C_{nm}$
factor in the basis definition~\eqref{eq:gnm_full}. Following the
same Fourier-domain evaluation yields
\begin{align}\label{eq:wn0}
  w_{n0} = 2\,\Re\left[\sum_{l=1}^{N_t/2}\tilde{x}_l\,e^{4\pi i l
  n/N_t}\,\tilde{\varphi}(l\,df)\right] + \tilde{x}_0\,\tilde{\varphi}(0)\,.
\end{align}
Note the factor of $4\pi$ in the exponent (versus $2\pi$ for interior
channels): the DC basis element oscillates at twice the rate in the
time index because it sees both positive and negative frequency
copies of the window centered at $f=0$.

\subsubsection{Nyquist channel ($m = N_f$)}

The Nyquist edge channel is handled analogously to the DC channel but
centered at the Nyquist frequency $f_{\rm max}$. Its structure
mirrors the DC case with appropriate frequency shifts. In practice,
the Nyquist channel is computed using the same windowed-FFT machinery
as the interior channels, with the frequency shift set to $mN_t/2 = N/2$.
A concrete toy example is shown in Figure~\ref{fig:wdm_overview},
which compares the same signal in the time, Fourier, and packed WDM
domains.

\begin{figure*}[t]
  \centering
  \includegraphics[width=0.88\linewidth]{figures/wdm_overview.pdf}
  \caption{\textbf{A simple signal seen in three representations.}
    The top panel shows a toy signal in the time domain, the middle
    panel shows its Fourier amplitude spectrum, and the bottom panel
    shows the magnitude of the packed WDM coefficients. The WDM
    representation keeps the narrow-band oscillation localized in
  frequency while also localizing the transient chirping feature in time.}
  \label{fig:wdm_overview}
  \script{wdm_overview.py}
\end{figure*}

\subsection{Backward transform}

The original data stream is recovered by applying the backward
(inverse) WDM transform to the coefficients:
\begin{align}
  \label{eq:inverse_wavelet_decomposition}
  x[k]=\sum_{n=0}^{N_t-1}\sum_{m=0}^{N_f}w_{nm}\,g_{nm}[k]\,.
\end{align}
Equivalently, in the Fourier domain the reconstruction reads
\begin{align}\label{eq:inverse_fourier}
  \tilde{x}_l = \sum_{n=0}^{N_t-1}\sum_{m=0}^{N_f}\tilde{g}_{nm}[l]\,w_{nm}\,.
\end{align}
That this is indeed the inverse of the forward transform follows from
the (quasi-)orthonormality of the basis: inserting the forward
transform~\eqref{wnm_def} into~\eqref{eq:inverse_fourier} and
using~\eqref{eq:orthonormality} returns $\tilde{x}_l$ up to the
overlap of adjacent window functions. Specifically, the sum over $n$
produces $N_t\delta_{ll'}$ factors via the discrete orthogonality of
the complex exponentials, and the sum over $m$ covers the full
frequency axis because adjacent windows satisfy
$\tilde{\varphi}^2(\omega) + \tilde{\varphi}^2(\omega - \Delta\Omega)
= 1/\Delta\Omega$ in their overlap region (proven in
Appendix~\ref{app:normalization_Meyer}). Together these two
identities ensure exact round-trip reconstruction: forward followed
by inverse returns the original data to machine precision, as
verified numerically in Section~\ref{sec:code_package}.

\subsection{Statistical properties and the WDM likelihood}\label{sec:stats}

The practical payoff of the (approximate) orthonormality and
decorrelation discussed above is that standard gravitational-wave
data analysis quantities --- the likelihood, signal-to-noise ratio,
and Fisher information matrix --- can all be written as simple sums
over WDM pixels, in direct analogy with the familiar frequency-domain
expressions.

\paragraph{From covariance to likelihood.}
For Gaussian noise characterized by a covariance matrix $\mathbf{C}$,
the log-likelihood of the data $\mathbf{d}$ given a signal model $\mathbf{h}$ is
\begin{align}
  \ln p(\mathbf{d}|\mathbf{h}) =
  -\frac{1}{2}(\mathbf{d}-\mathbf{h})^\top
  \mathbf{C}^{-1}(\mathbf{d}-\mathbf{h}) - \frac{1}{2}\ln\det(2\pi\mathbf{C})\,.
\end{align}
In the Fourier domain with stationary noise, $\mathbf{C}$ is diagonal
with entries proportional to the power spectral density $S(f_l)$, so
the likelihood reduces to a sum over frequency bins --- the Whittle
likelihood. The same simplification occurs in the WDM domain whenever
the noise covariance is (approximately) diagonal, which, as argued
above, holds for locally stationary noise whose PSD varies slowly
compared to the frequency support of each atom. In that regime the
WDM noise covariance takes the form
\begin{align}\label{eq:wdm_noise_cov}
  \langle w_{nm}^{\mathrm{noise}}\, w_{pq}^{\mathrm{noise}} \rangle
  \approx S_{nm}\,\delta_{np}\,\delta_{mq}\,,
\end{align}
where $S_{nm}$ is the \emph{evolutionary power spectral density}
evaluated at the time-frequency location of pixel $(n,m)$. For
stationary noise, $S_{nm}$ reduces to the usual PSD integrated over
the atom's frequency support~\cite{Cornish:2020odn}:
\begin{align}\label{eq:snm_stationary}
  S_{nm} \approx S(f_m)\,\Delta F\,.
\end{align}
Substituting the diagonal covariance~\eqref{eq:wdm_noise_cov} into
the general Gaussian likelihood yields the WDM Whittle log-likelihood:
\begin{align}\label{eq:wdm_whittle}
  \ln p(d|h) = -\frac{1}{2}\sum_{n,m}\left[\ln(2\pi S_{nm}) +
  \frac{(d_{nm} - h_{nm})^2}{S_{nm}}\right],
\end{align}
where $d_{nm}$, $h_{nm}$ are the WDM coefficients of the data and
signal model, respectively. This expression is structurally identical
to the standard frequency-domain Whittle likelihood, but with the
one-dimensional sum over frequency bins replaced by a two-dimensional
sum over time-frequency pixels. The key advantage is that $S_{nm}$
can vary with time, naturally accommodating non-stationary noise
without requiring off-diagonal terms in the covariance.

\paragraph{Inner product, SNR, and Fisher matrix.}
The noise-weighted inner product in the WDM domain follows directly
from~\eqref{eq:wdm_whittle}:
\begin{align}\label{eq:wdm_inner}
  (a \,|\, b) = \sum_{n,m} \frac{a_{nm}\,b_{nm}}{S_{nm}}\,,
\end{align}
from which the matched-filter signal-to-noise ratio is
\begin{align}
  \rho(h) = \frac{(d \,|\, h)}{\sqrt{(h \,|\, h)}}\,,
\end{align}
and the Fisher information matrix is
\begin{align}\label{eq:wdm_fisher}
  \Gamma_{ij} = \sum_{n,m} \frac{\partial_{i} h_{nm}\;\partial_{j}
  h_{nm}}{S_{nm}}\,.
\end{align}
These expressions assume that the diagonal
approximation~\eqref{eq:wdm_noise_cov} holds. When it does not ---
for example, when the noise PSD varies rapidly compared to $\Delta
T$, or when the noise is strongly non-Gaussian --- the off-diagonal
covariance terms must be retained and the simple pixel-wise sums
above become inadequate. Figure~\ref{fig:wdm_orthogonality_decorrelation}
provides a practical
diagnostic: if the off-diagonal entries of the empirical covariance
remain small after whitening, the diagonal
likelihood~\eqref{eq:wdm_whittle} is a safe approximation.

\paragraph{When does the diagonal approximation hold?}
The diagonal covariance requires two conditions to be satisfied
simultaneously. First, the noise must be \emph{locally stationary}:
its PSD must vary on timescales much longer than the wavelet pixel
width $\Delta T$. Second, the PSD must be approximately flat across
the frequency support of each atom, which spans roughly $\Delta F$
for interior channels. The Heisenberg--Gabor uncertainty relation,
saturated by the WDM tiling at $\Delta T\,\Delta F = 1/2$, means
these two conditions trade off against each other. Choosing a finer
frequency resolution (large $\Delta T$) improves the second condition
but worsens the first. In practice, a single choice of $N_f$ often
suffices for mildly non-stationary noise, but strongly non-stationary
data with sharp spectral features may require a multi-resolution
strategy, as discussed by Cornish~\cite{Cornish:2020odn}.

\section{\package\ Package}
\label{sec:code_package}

The \package\ package provides practical implementations of the
Wilson–Daubechies–Meyer (WDM) transform for gravitational-wave data
analysis. The current codebase exposes a reference NumPy backend and
a vectorized JAX backend; both implement the same packed $(N_t, N_f +
1)$ coefficient layout and the same forward/inverse conventions described above.

From the implementation point of view, the dominant operations in the
forward transform are one length-$N$ FFT of the input data together
with one length-$N_t$ sub-band transform for each stored channel.
This gives a cost of
\begin{align}
  T_{\rm forward} = \mathcal{O}(N \log N) + \mathcal{O}(N \log N_t),
\end{align}
which is asymptotically $\mathcal{O}(N \log N)$ because $N=N_tN_f$
and $N_t \leq N$. The inverse transform has the same scaling up to
constant factors: it reconstructs the full spectrum from the packed
channels and then applies one inverse FFT back to the time domain. In
both directions, the stored coefficient grid itself contains
$\mathcal{O}(N)$ real numbers, and the temporary working arrays used
by the implementation are also linear in $N$, so the overall memory
footprint is $\mathcal{O}(N)$.

In practice, the important implementation difference is not the
asymptotic scaling but how the channel-wise work is executed. The
NumPy backend carries out the sub-band operations on the CPU, whereas
the JAX backend evaluates the same algebra in a more vectorized form
that benefits from accelerator execution. Below we use the current
checked-in benchmark snapshot from the package documentation, which
compares the NumPy and JAX backends across a range of input sizes.
All results are obtained in 64-bit floating point precision, and each
timed point uses one warmup evaluation before the measured runs. The
hardware specifications for the benchmark machine are:

\begin{itemize}
  \item \textbf{GPU:} 1× Tesla K80 (compute capability 3.7, 2496 CUDA
    cores, 12 GB GDDR5 VRAM)
  \item \textbf{CPU:} 1× single-core hyper-threaded Intel Xeon
    Processor @ 2.3 GHz (1 core, 2 threads)
  \item \textbf{RAM:} approximately 12 GB
\end{itemize}

Figure~\ref{fig:runtime_forward} shows the forward WDM runtime as a
function of data length for the current NumPy and JAX implementations.

\begin{figure}[tbp]
  \centering
  \includegraphics[width=1\linewidth]{figures/runtimes.pdf}
  \caption{\textbf{Forward WDM runtime versus data length.} Each
    point shows the mean runtime over repeated runs after one warmup
    call, and the shaded band shows one standard deviation. The dashed
    lines show least-squares fits of the form $c\,N\log_2 N$, included
    as scaling guides. The JAX backend is consistently faster than the
    NumPy backend, with the separation becoming more pronounced for the
  largest transform sizes in the benchmark range.}
  \label{fig:runtime_forward}
  \script{benchmark_runtime_snapshot.py}
\end{figure}

As visible in Figure~\ref{fig:runtime_forward}, both implementations
become slower as $N$ increases, but the JAX backend maintains a
noticeably lower runtime across the full benchmark range. The
difference is modest for small problems and becomes much larger once
the transform size reaches the upper end of the tested range.
Figure~\ref{fig:mse_boxplot} complements this by summarizing the
round-trip reconstruction error through the maximum absolute
difference after a forward transform followed by the inverse transform.

\begin{figure}[tbp]
  \centering
  \includegraphics[width=1\linewidth]{figures/mse.pdf}
  \caption{\textbf{Round-trip reconstruction error versus data
    length.} The plotted quantity is the maximum absolute difference
    between the input signal and the reconstructed signal after
    applying the forward and inverse WDM transforms. Both backends
  remain in the roundoff-level regime across the full benchmark range.}
  \label{fig:mse_boxplot}
  \script{benchmark_error_snapshot.py}
\end{figure}

Both benchmarked backends achieve reconstruction errors consistent
with floating-point roundoff. In other words, the main runtime
differences visible in Figure~\ref{fig:runtime_forward} are not
coming from a loss of numerical fidelity in the transform itself.

To illustrate the utility of \package\ on astrophysically relevant
signals, we transform three different gravitational-wave datasets
into the WDM domain. Specifically, we transform (i) an \emph{extreme
mass-ratio inspiral} (EMRI) waveform, (ii) a \emph{massive black hole
binary} (MBHB) signal and (iii) a set of \emph{verification
galactic binaries} (VGB).
The MBHB and VGB data are obtained from the Radler LISA Data
Challenge (via \url{https://lisa-ldc.lal.in2p3.fr/challenge1a}).
The EMRI signal is generated using the \textsc{few} waveform model
\textsc{Pn5AAKWaveform}.
Figure~\ref{fig:wdm_real_waveforms} displays both the
frequency-domain characteristic strain and the WDM-domain
representations of these three signals. Each panel shows the
time-frequency amplitude map obtained with the JAX implementation.

\begin{figure*}[t]
  \centering
  \includegraphics[width=1\linewidth]{figures/example_transforms.pdf}
  \caption{WDM-domain representations of three astrophysical signals:
    (a) EMRI waveform; (b) massive black hole binary from the Radler
    LISA Data Challenge; (c) verification binary ensemble. All
  transforms computed with JAX on the GPU.}
  \label{fig:wdm_real_waveforms}
\end{figure*}

\subsection{Resolved galactic-binary inference in a galactic background}
\label{sec:lisa_gb_example}

As an end-to-end inference example, we generated a one-year LISA-like
dataset containing stationary instrument noise, a stochastic galactic
foreground, and one resolved galactic binary (GB), and then analyzed
that realization with matched frequency-domain and WDM-domain
likelihoods. Figure~\ref{fig:lisa_gb_example} shows a representative
realization together with the overlaid posterior constraints from the
two analyses.

The data set is constructed on a frequency grid spanning
$10^{-4}$--$3\times 10^{-3}\,\mathrm{Hz}$. The unresolved galactic
foreground is described by the phenomenological PSD
\begin{align}
  S_{\rm gal}(f) =
  A_{\rm gal} f^{-7/3}
  \exp\!\left[-\left(\frac{f}{f_{1,{\rm gal}}}\right)^{\alpha_{\rm gal}}\right]
  \left[1 + \tanh\!\left(\frac{f_{{\rm knee},{\rm gal}} - f}{f_{2,{\rm gal}}}\right)\right],
\end{align}
using a standard smooth fit for the LISA galactic foreground. To
project this foreground into the detector channels we combine that
frequency model with an anisotropic Milky Way sky map obtained by
line-of-sight integration through the bulge-plus-disk density model
\begin{align}
  \rho_{\rm gal}(x,y,z) =
  \rho_0\left[
    A e^{-r^2/R_b^2}
    + (1-A)e^{-u/R_d}\,{\rm sech}^2(z/Z_d)
  \right],
\end{align}
where $r^2=x^2+y^2+z^2$ and $u^2=x^2+y^2$. We evaluate the
time-dependent detector response using equal-arm LISA orbits sampled
at $90$ orbital times, and use that response to map the anisotropic
foreground into the A/E/T TDI channels. We then draw complex Gaussian
realizations of both the instrumental noise and the chunked galactic
foreground and stitch them into a full-year spectrum. The resolved
source is injected afterward as a TDI 1.5 A/E/T waveform with
equal-arm LISA orbits and local Fourier support $n=256$. In the
realization shown here the cadence is
$\Delta t = 166.7\,\mathrm{s}$, $T_{\rm obs}=1\,\mathrm{yr}$, and the
injected binary has
$f_0 = 1.3862897\,\mathrm{mHz}$,
$\dot f = 1.32\times 10^{-18}\,\mathrm{Hz\,s^{-1}}$,
$A = 1.19\times 10^{-23}$, and
$\phi_0 = 1.442\,\mathrm{rad}$.

The resolved GB model uses the standard eight source parameters
$(f_0,\dot f,A,\alpha,\delta,\psi,\iota,\phi_0)$, but for this
controlled comparison we sample only
$(f_0,\dot f,A,\phi_0)$ and hold sky position and orientation fixed to
their injected values. The carrier frequency is drawn near the fixed
reference value $f_{0,\rm ref}=1.38591\,\mathrm{mHz}$ through
$\delta\!\ln f_0 \sim \mathcal{U}[-10^{-3},10^{-3}]$, while
$\dot f$ and $A$ are drawn from truncated Gaussians in log space over
$[5\times 10^{-19},\,4\times 10^{-18}]\,\mathrm{Hz\,s^{-1}}$ and
$[6\times 10^{-24},\,1.7\times 10^{-23}]$, respectively.

The frequency-domain analysis uses a local A/E/T band centered on the
prior support of the source and evaluates the Whittle likelihood
\begin{align}
  \ln \mathcal{L}_{\rm freq}(\theta)
  \propto
  -\sum_{c\in\{A,E,T\}} \sum_k
  \left[
    \ln S_c(f_k)
    + \frac{2\Delta f\,\left|\Delta t\,[d_{c,k}-h_{c,k}(\theta)]\right|^2}{S_c(f_k)}
  \right],
\end{align}
where $d_{c,k}$ and $h_{c,k}$ are the complex local Fourier modes and
$S_c(f)$ is the stationary effective PSD, namely instrumental noise
plus the time-averaged galactic
foreground contribution in channel $c$. The WDM analysis starts from
the same local Fourier patch, embeds the template into that
patch, and applies the WDM transform with
$N_t=32$, $a=1/3$, and $d=1$. The resulting WDM likelihood is the
diagonal Gaussian specialization of Eq.~\eqref{eq:wdm_whittle},
\begin{align}
  \ln \mathcal{L}_{\rm WDM}(\theta)
  =
  -\frac{1}{2}\sum_{c\in\{A,E,T\}} \sum_{n,m}
  \left[
    \ln(2\pi \sigma^2_{c,m})
    + \frac{\left(w^{(d)}_{c,nm}-w^{(h)}_{c,nm}(\theta)\right)^2}{\sigma^2_{c,m}}
  \right],
\end{align}
with a stationary per-pixel variance
\begin{align}
  \sigma^2_{c,m} = \frac{S_c(f_m)}{2\Delta t} = f_{\rm Nyq} S_c(f_m),
\end{align}
broadcast across the WDM time bins. In other words, this example uses
the same stationary PSD in both domains and asks whether the WDM
transform reproduces the frequency-domain posterior under that shared
noise model.

Both analyses use the NumPyro implementation of the No-U-Turn Sampler
with a dense mass matrix, target acceptance probability $0.85$, and
maximum tree depth $10$. Each run uses $800$ warmup steps followed by
$1000$ retained samples from a single chain, initialized near the
injected parameters with small jitters in $\log \dot f$ and $\log A$.
In the realization shown here, both samplers produced zero divergences. The
latent effective sample sizes are $613$--$637$ for the
frequency-domain chain and $590$--$714$ for the WDM chain, while the
corresponding $\hat R$ values lie in the ranges
$0.999$--$1.007$ and $0.999$--$1.016$. The truth-band optimal SNRs are
$22.5469$ in the frequency-domain calculation and $22.5462$ in the
WDM calculation. All reported $90\%$ credible intervals contain the
injected truth in both runs, and the posterior medians differ only by
$9.0\times 10^{-11}\,\mathrm{Hz}$ in $f_0$,
$2.2\times 10^{-20}\,\mathrm{Hz\,s^{-1}}$ in $\dot f$,
$5.2\times 10^{-26}$ in $A$,
$6.1\times 10^{-3}\,\mathrm{rad}$ in $\phi_0$, and
$0.10$ in SNR. Within the numerical precision of this study, the WDM
and frequency-domain analyses are therefore statistically
indistinguishable for this seeded resolved-GB example.

\begin{figure}[t]
  \centering
  \includegraphics[width=\linewidth]{figures/lisa_gb_demo_data.pdf}
  \caption{\textbf{LISA galactic background: Strain comparison.} Characteristic-strain comparison between the injected resolved galactic binary and the channel noise model including the galactic foreground.}
  \label{fig:lisa_gb_data}
  \script{lisa_gb_data.py}
\end{figure}

\begin{figure}[t]
  \centering
  \includegraphics[width=\linewidth]{figures/lisa_gb_demo_corner.pdf}
  \caption{\textbf{LISA galactic background: Posterior analysis.} Overlaid posterior corner plot for the WDM-domain and frequency-domain analyses of the same realization. The two posterior families overlap closely in $(f_0, \dot{f}, A, \phi_0)$ and in the derived optimal SNR.}
  \label{fig:lisa_gb_corner}
  \script{lisa_gb_corner.py}
\end{figure}


The \package\ package enables both a straightforward NumPy-based
development path and high-performance accelerator execution via JAX.
For small data lengths ($N \lesssim 2^{20}$), the NumPy version is
competitive or even faster due to its extremely low overhead.
However, once $N$ exceeds $\sim 2^{20}$, the GPU-accelerated versions
achieve markedly lower runtimes while maintaining MSE well below $10^{-8}$.
This combination of flexibility, accuracy, and performance makes
\package\ a useful tool for time-frequency-WDM analysis of
long-duration gravitational-wave signals.

\section{Conclusions}\label{sec:conclusions}

We have presented a self-contained, pedagogical account of the
Wilson-Daubechies-Meyer (WDM) wavelet packet transform and its
implementation in the open-source \package\ package. The main goal of
this work is to make the WDM representation accessible to readers who
are new to time-frequency methods, by walking through the full
three-case basis definition (DC, interior, and Nyquist channels), the
packed $(N_t, N_f{+}1)$ coefficient layout, the forward and inverse
transforms with detailed derivations, and the conditions under which
WDM coefficients can be treated as approximately independent.

On the implementation side, the NumPy and JAX backends both achieve
exact round-trip reconstruction to floating-point precision. The JAX
backend provides substantially lower runtimes for large transforms,
making it practical to incorporate WDM-domain operations into
inference pipelines that require millions of likelihood evaluations.

Several limitations of the current work should be noted. First, the
package implements only the $d=1$ Meyer window; higher-order windows
($d > 1$) offer better spectral leakage suppression and may be
preferable for data with sharp spectral features. Second, this paper
does not address the generation of gravitational-wave signals
directly in the WDM domain, which is where much of the computational
advantage of the WDM representation lies~\cite{Cornish:2020odn}; we
defer fast wavelet-domain waveform generation to future work. Third,
the treatment of data gaps and edge effects, while straightforward in
principle, requires careful handling of the boundary wavelets and is
not yet implemented in the package.

Looking ahead, the most immediate extensions are the incorporation of
fast wavelet-domain waveform generation for compact binary signals,
the implementation of evolutionary power spectral density estimation
directly on the WDM grid, and integration of the package into
existing LISA data analysis frameworks. The combination of a
pedagogical reference, a tested open-source implementation, and
GPU-accelerated performance should lower the barrier for the
gravitational-wave community to adopt time-frequency methods for the
long-duration, non-stationary signals expected from future detectors.

\section*{Data and Software Availability}
The software developed for this research is available on GitHub at
\url{https://github.com/pywavelet/wdm_transform}. Documentation and
worked examples are available in the repository documentation at
\url{https://github.com/pywavelet/wdm_transform/tree/main/docs}.

\begin{acknowledgments}
  GM acknowledges support from the Imperial College London
  Schr\"odinger Scholarship scheme.
\end{acknowledgments}

\appendix
\onecolumngrid

\section{Detailed forward transform derivation}\label{app:forward_derivation}

This appendix provides the full algebraic steps leading to the
compact expressions for the forward WDM coefficients presented in
Section~\ref{sec:wdm}. We work in the Fourier domain throughout,
writing the forward transform as
\begin{align}
  w_{nm}=\sum_{l=0}^{N-1} \tilde{x}_l\,\tilde{g}_{nm}^*(f[l])\,,
\end{align}
where the discrete frequencies are mapped as
\begin{align}
  f[l]=
  \begin{cases}
    l & 0\leq l< N/2 \\
    l-N & N/2\leq l<N
  \end{cases}\times df\,,\qquad df = \frac{1}{T}\,.
\end{align}

\subsection{Interior channels ($m > 0$)}

Inserting the interior-channel basis element from
Eq.~\eqref{eq:gnm_full} and splitting the sum over positive and
negative frequencies gives
\begin{align}
  w_{nm}&=\frac{1}{\sqrt{2}}\sum_{l=0}^{N/2-1} \tilde{x}_l\, e^{2\pi
  iln/N_t}\left[C_{nm}^*\tilde{\varphi}\!\left(\!(l-\tfrac{mN_t}{2})df\right)+C_{nm}\tilde{\varphi}\!\left(\!(l+\tfrac{mN_t}{2})df\right)\right]\nonumber\\
  &+\frac{1}{\sqrt{2}}\sum_{l=N/2+1}^{N-1} \tilde{x}_l\, e^{2\pi
  i(l-N)n/N_t}\left[C_{nm}^*\tilde{\varphi}\!\left(\!(l\!-\!N\!-\!\tfrac{mN_t}{2})df\right)+C_{nm}\tilde{\varphi}\!\left(\!(l\!-\!N\!+\!\tfrac{mN_t}{2})df\right)\right].
\end{align}
Substituting $l \to N-l$ in the second sum and using the reality
condition $\tilde{x}_{N-l} = \tilde{x}_l^*$ together with the
symmetry $\tilde{\varphi}(-f) = \tilde{\varphi}(f)$, the second sum
is the complex conjugate of the first. The boundary terms at $l=0$
and $l=N/2$ vanish because the window support does not extend to
those frequencies for interior channels. We therefore obtain
\begin{align}\label{eq:app_wnm_general}
  w_{nm}&=\sqrt{2}\,\Re\sum_{l=1}^{N/2-1} \tilde{x}_l\, e^{2\pi
  iln/N_t}\,C_{nm}^*\,\tilde{\varphi}\!\left(\!(l-\tfrac{mN_t}{2})df\right)\nonumber\\
  &=\sqrt{2}\,(-1)^{nm}\,\Re\sum_{l'=-N_t/2}^{N_t/2}
  \tilde{x}_{l'+mN_t/2}\, e^{2\pi
  il'n/N_t}\,C_{nm}^*\,\tilde{\varphi}(l'\,df)\,,
\end{align}
where the last equality follows from the substitution $l' = l -
mN_t/2$ and the fact that $\tilde{\varphi}(l'\,df)$ is nonzero only
for $|l'| \leq N_t/2$. This is Eq.~\eqref{eq:wnm_interior} of the
main text, and corresponds (up to normalization conventions) to
Eq.~17 of Cornish~\cite{Cornish:2020odn}.

\subsection{DC channel ($m = 0$)}

For the DC edge channel, the basis element has a doubled time-shift
exponent and no $C_{nm}$ factor. The same positive/negative frequency
splitting yields
\begin{align}
  w_{n0}&=\sum_{l=0}^{N/2-1} \tilde{x}_l\, e^{4\pi
  iln/N_t}\,\tilde{\varphi}(l\,df)
  +\sum_{l=N/2}^{N-1} \tilde{x}_l\, e^{4\pi
  i(l-N)n/N_t}\,\tilde{\varphi}((l-N)\,df)
  \nonumber\\
  &=2\,\Re\left[\sum_{l=1}^{N_t/2} \tilde{x}_{l}\, e^{4\pi
  iln/N_t}\,\tilde{\varphi}(l\,df)\right]+\tilde{x}_0\,\tilde{\varphi}(0)\,,
\end{align}
which is Eq.~\eqref{eq:wn0} of the main text.

\section{Normalization of the Meyer window}\label{app:normalization_Meyer}

We verify that the Meyer window defined in
Eq.~\eqref{eq:meyer_window} is correctly normalized. Specializing to
the $d=1$ case used throughout this work and defining $a =
A/\Delta\Omega$ with $B = \Delta\Omega - 2A$, we compute
\begin{align}
  \int_{-\infty}^{+\infty} d\omega\, \tilde{\varphi}^2(\omega)
  &= 2\int_{0}^{A}\frac{d\omega}{\Delta\Omega}
  +
  2\int_{A}^{A+B}\frac{d\omega}{\Delta\Omega}\,\cos^2\!\left[\frac{\pi}{2}\left(\frac{\omega-A}{B}\right)\right]\nonumber\\
  &= 2a + 2(1-2a)\int_0^1 dz\,\cos^2\!\left[\frac{\pi}{2}\,z\right]\nonumber\\
  &= 2a + (1-2a)\left(1 + \underbrace{\int_0^1
  dz\,\cos[\pi\,z]}_{=\,0}\right)\nonumber\\
  &= 1\,,
\end{align}
where the substitution $z = (\omega - A)/B$ was used. This unit
normalization, together with the overlap identity
$\tilde{\varphi}^2(\omega) + \tilde{\varphi}^2(\omega - \Delta\Omega)
= 1/\Delta\Omega$ in the transition region, is what guarantees exact
round-trip reconstruction of the forward-inverse transform pair.

\bibliographystyle{plain}
\bibliography{bib}

\end{document}
